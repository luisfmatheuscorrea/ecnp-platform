export interface LogoProps {
  margin?: string;
  size?: string;
  fontSize?: string;
  noLabel?: boolean;
  short?: boolean;
}

export interface LogoContainerProps {
  margin?: string;
  fontSize?: string;
}
