export interface HeaderProps {
  margin?: string;
  size?: string;
  fontSize?: string;
  noLabel?: boolean;
  short?: boolean;
}

export interface HeaderContainerProps {
  margin?: string;
  fontSize?: string;
}
